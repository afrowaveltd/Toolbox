namespace Afrowave.Toolbox.Essentials.Tests;

/// <summary>
/// Smoke test — verifies that the test infrastructure is wired up correctly.
/// Replace with real tests once the package content is implemented.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void TrueIsTrue()
    {
        Assert.True(true);
    }
}
